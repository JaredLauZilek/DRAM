# Copy to .env and fill in. Both are exposed to the browser (Vite VITE_ prefix),
# so only ever put the PUBLIC anon key here — never the service-role key.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_public_anon_key
